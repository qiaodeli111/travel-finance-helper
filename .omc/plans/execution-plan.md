# Travel Finance Helper - 执行计划

## 文档信息
- **版本**: v2.0
- **日期**: 2026-03-02
- **基于**: PRD-v2.0.md

---

## 用户确认的需求调整

| 问题 | 用户决定 |
|------|----------|
| 老用户访问处理 | 无账本→引导；有账本→不引导；有账本未登录→显示登录界面 |
| 归档数据保留 | 永久保留 |
| PDF导出功能 | 移到设置功能里，不删除 |
| 实施顺序 | 按优先级顺序 |

---

## 阶段1: P0 关键需求 (同步修复 + 首次引导)

### 任务 1.1: 修复双向同步
**状态**: 待执行
**文件**: `src/contexts/CloudSyncContext.tsx`, `services/firestoreService.ts`

**步骤**:
1. 修改 `syncNow` 方法，增加从云端拉取费用的逻辑
2. 实现费用合并策略（云端数据优先，保留本地新增）
3. 修复 `subscribeToExpenses` 订阅回调，确保正确更新本地状态
4. 在 `InviteModal.tsx` 的 `onJoinSuccess` 中确保触发数据拉取

**验收**:
- [ ] 用户A添加费用，用户B在3秒内看到
- [ ] 加入账本后立即显示所有历史费用
- [ ] 离线添加的费用联网后自动同步

---

### 任务 1.2: 首次使用引导界面
**状态**: 待执行
**文件**: `App.tsx`, `components/WelcomeWizard.tsx`

**步骤**:
1. 修改 `App.tsx` 的 `initApp` 逻辑：
   - 检查是否有账本
   - 无账本 → 显示引导界面
   - 有账本未登录 → 显示登录界面
   - 有账本已登录 → 直接进入上次账本

2. 完善 `WelcomeWizard.tsx`：
   - 步骤1: 选择出发地（国家）
   - 步骤2: 选择目的地（国家）
   - 步骤3: 设置参与者（家庭/成员）
   - 步骤4: 设置预算（可选）
   - 步骤5: 登录/游客模式选择

3. 创建 `AuthChoiceModal.tsx`：
   - 显示登录和游客模式选项
   - 游客模式提示数据仅存本地

**验收**:
- [ ] 新用户显示完整引导流程
- [ ] 有账本的老用户直接进入
- [ ] 有账本未登录用户显示登录界面

---

## 阶段2: P1 高优先级需求

### 任务 2.1: 费用明细显示创建者
**状态**: 待执行
**文件**: `types.ts`, `components/ExpenseList.tsx`, `components/ExpenseForm.tsx`

**步骤**:
1. 扩展 `Expense` 接口，添加 `createdBy`, `createdByDisplayName`, `createdAt` 字段
2. 修改 `ExpenseForm.tsx`，创建费用时记录创建者信息
3. 修改 `ExpenseList.tsx`，显示 "由 XXX 添加于 YYYY-MM-DD HH:mm"
4. 修改 `CloudSyncContext.tsx` 的数据转换，确保字段同步

**验收**:
- [ ] 每条费用显示创建者信息
- [ ] 云端数据正确存储创建者
- [ ] 旧数据兼容（无创建者时不显示）

---

### 任务 2.2: 邀请码加入流程优化
**状态**: 待执行
**文件**: `components/InviteModal.tsx`, `App.tsx`

**步骤**:
1. 在账本列表菜单中突出显示"加入账本"入口
2. 点击后直接显示邀请码输入框（无需额外点击）
3. 输入邀请码 → 验证 → 显示账本信息确认 → 加入
4. 加入成功后立即拉取数据并切换到该账本

**验收**:
- [ ] 用户可通过输入邀请码直接加入
- [ ] 加入流程不超过3步
- [ ] 加入后立即显示账本数据

---

### 任务 2.3: 账本删除和归档
**状态**: 待执行
**文件**: `types/firestore.ts`, `components/LedgerManagePanel.tsx`, `services/firestoreService.ts`

**步骤**:
1. 扩展 `CloudLedger` 接口，添加 `status`, `archivedAt`, `archivedBy` 字段
2. 在 `firestoreService.ts` 添加：
   - `archiveLedger(ledgerId, userId)` - 归档账本
   - `unarchiveLedger(ledgerId, userId)` - 取消归档
   - `deleteLedger(ledgerId)` - 已存在，确保删除所有关联数据

3. 修改 `LedgerManagePanel.tsx`：
   - 添加归档按钮（仅创建者可见）
   - 添加删除按钮（仅创建者可见）
   - 显示归档状态

4. 修改 `App.tsx`：
   - 归档账本隐藏添加费用按钮
   - 归档状态实时同步

**验收**:
- [ ] 创建者可删除账本
- [ ] 创建者可归档/取消归档
- [ ] 归档后所有成员无法添加费用
- [ ] 归档数据永久保留

---

### 任务 2.4: 游客模式转正式用户
**状态**: 待执行
**文件**: `src/contexts/AuthContext.tsx`, `App.tsx`

**步骤**:
1. 在 `AuthContext.tsx` 添加登录成功后的本地账本检测
2. 创建 `MigrationPrompt.tsx` 组件：
   - 显示本地账本列表
   - 用户选择要迁移的账本
   - 迁移到云端

3. 修改登录流程：
   - 登录成功 → 检测本地账本 → 显示迁移提示 → 迁移

**验收**:
- [ ] 游客模式明确标识
- [ ] 登录后自动提示迁移
- [ ] 迁移成功后可共享

---

## 阶段3: P2 中优先级需求

### 任务 3.1: 账本列表显示创建者
**状态**: 待执行
**文件**: `App.tsx`, `components/LedgerManagePanel.tsx`

**步骤**:
1. 扩展 `LedgerMeta` 接口，添加 `ownerId`, `ownerDisplayName`, `isLocal` 字段
2. 在账本列表中显示创建者头像/名称
3. 本地账本显示"本地"标签

**验收**:
- [ ] 云端账本显示创建者
- [ ] 本地账本显示"本地"标签

---

## 阶段4: P3 低优先级需求

### 任务 4.1: 移动导出功能到设置
**状态**: 待执行
**文件**: `App.tsx`, `components/SettingsModal.tsx`

**步骤**:
1. 删除 `App.tsx` 底部的备份/恢复/Markdown导出按钮
2. 将导出功能移到 `SettingsModal.tsx`：
   - 导出PDF
   - 导出Markdown
   - 备份数据(JSON)
   - 恢复数据(JSON)

**验收**:
- [ ] 底部不再显示导出按钮
- [ ] 设置中有完整的导出选项

---

## 执行进度追踪

| 阶段 | 任务 | 状态 | 完成日期 |
|------|------|------|----------|
| 1.1 | 修复双向同步 | 待执行 | - |
| 1.2 | 首次使用引导 | 待执行 | - |
| 2.1 | 费用明细创建者 | 待执行 | - |
| 2.2 | 邀请码加入优化 | 待执行 | - |
| 2.3 | 删除和归档 | 待执行 | - |
| 2.4 | 游客转正式 | 待执行 | - |
| 3.1 | 账本创建者显示 | 待执行 | - |
| 4.1 | 移动导出功能 | 待执行 | - |

---

## 实施注意事项

1. **数据迁移**: 所有接口扩展需要兼容旧数据
2. **实时同步**: Firestore 订阅需要正确处理离线/在线切换
3. **权限控制**: 删除/归档操作需要验证用户权限
4. **测试覆盖**: 每个任务完成后需要验证验收标准